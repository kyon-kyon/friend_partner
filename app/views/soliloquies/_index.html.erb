

<div class="container">
  <div class="row">
    <div class="col-md-2 col-md-offset-2", style="background-color: #EEEEEE;">
      <h4 class="text-center">つぶやき？</h4>
    </div>
  </div>
	<br>
	<br>
	<div class="row">
  <table class='table table-hover table-inverse'>
  <thead>
    <tr>
      <th></th>
      <th>つぶやきタイトル</th>
      <th>time</th>
      <th colspan="3">いいね数</th>
    </tr>
  </thead>

  <tbody>
    <% soliloquies.each do |soliloquy| %>
      <tr>

        <td><%#= link_to user_path(book.user) do %>
          <%#= attachment_image_tag(book.user, :profile_image, fallback: "no-image-icon.jpg", size: "50x50") %>
          <%# end %>
          <%= link_to soliloquy.title, soliloquy_path(soliloquy.id) %>
        </td>
        <td>
            <%= soliloquy.created_at %>
            <%#= link_to book.title, book_path(book), class: "book_#{book.id}" %></td>
        <td><%#= book.body %></td>
      </tr>
    <% end %>
  </tbody>
</table>




<div class="">
    <table class="text-center table center-index">
      <thead>
        <tr>
          <th class="text-center"></th>
          <th class="text-center">つぶやきタイトル</th>
          <th class="text-center">ユーザーページへ</th>
          <th class="text-center">何を思ったか</th>
          <th class="text-center">ユーザー名</th>
        </tr>
      </thead>


        <% soliloquies.each do |soliloquy| %>
          <tbody>
            <tr>
              <td><%= attachment_image_tag soliloquy.user, :profile_image, fallback: "no_image.jpg", size: "50x50" %></td>
              <td><%= soliloquy.user.is_deleted %></td>
              <td class="btn btn-default"><%= link_to "詳細", user_path(soliloquy.user) %></td>
              <td><%= soliloquy.how_feel %></td>
              <td><%= soliloquy.user.name %></td>
            </tr>
        <% end %>
  　</table>
</div>


